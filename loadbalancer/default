upstream myapp1 {
        # server minion_IP; 
        # server minion_IP;
        # server minion_IP;
}

server {
        listen 80;

        location / {
            proxy_pass http://myapp1;
        }
}
